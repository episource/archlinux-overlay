pkgname='dummy2'
pkgdesc='A dummy package for testing custom arch repository creation.'
pkgver='1.0.0'
pkgrel='1'
arch=('i686' 'x86_64')
license=('MIT')

depends=()
makedepends=()

package() {
    mkdir -p ${pkgdir}/usr/share/${pkgname}
    touch ${pkgdir}/usr/share/${pkgname}/dummy.file
}